import React from 'react';
import Hero from '../hero/hero';
import Card from '../card/card';

const Home = () => {
    return (
        <div>
            <Hero />
            <Card />
        </div>
    );
};

export default Home;
